# PostGIS connection (same as scripts/load_parcels_to_postgis.py)
# Option 1: individual vars
PARCEL_DB_HOST=localhost
PARCEL_DB_PORT=5432
PARCEL_DB_NAME=parcel_db
PARCEL_DB_USER=postgres
PARCEL_DB_PASSWORD=

# Option 2: single URL
# DATABASE_URL=postgresql://postgres:password@localhost:5432/parcel_db

# Real APN data from county ArcGIS (used when PostGIS has no row for the APN)
# Layer URL = Feature Server layer (e.g. .../FeatureServer/0), not /query
# PARCEL_ARCGIS_LAYER_URL=https://gis.hctx.net/arcgis/rest/services/Property/Property/FeatureServer/0
# Optional: APN field name if your county uses something other than apn/parcelno/acct_num
# PARCEL_ARCGIS_APN_FIELD=ACCT_NUM
