# Optional: Market Research live data (RapidAPI)
# Copy to .env and add your RapidAPI key; the key will be pre-filled in the app so you don't have to paste it each time.
# REACT_APP_MARKET_RESEARCH_API_KEY=your_rapidapi_key_here
# REACT_APP_MARKET_RESEARCH_API_URL=https://realty-in-us.p.rapidapi.com

# Optional: Regrid parcel map layer — see docs/REGRID.md
# Free 30-day sandbox: https://app.regrid.com/users/sign_up?flow=api_sandbox
# Raster: REACT_APP_REGRID_TILE_URL=https://tiles.regrid.com/.../{z}/{x}/{y}.png?key=YOUR_KEY
# Vector (.pbf): REACT_APP_REGRID_TILE_URL=https://tiles.regrid.com/parcels/{z}/{x}/{y}.pbf
# If the endpoint requires a key, add: REACT_APP_REGRID_API_KEY=your_key

# Optional: Free county/state parcel layer — see docs/PARCEL_LAYERS.md
# REACT_APP_PARCEL_TILE_URL=https://your-county.gov/.../MapServer/tile/{z}/{y}/{x}
# REACT_APP_PARCEL_WMS_URL=https://your-county.gov/.../MapServer/WMSServer
# REACT_APP_PARCEL_WMS_LAYERS=0

# Optional: County ArcGIS Feature Server (query by map bounds; free and powerful)
# Full URL to the layer, e.g. .../FeatureServer/0 (no /query). Example Harris County TX:
# REACT_APP_PARCEL_ARCGIS_FEATURESERVER_URL=https://gis.hctx.net/arcgis/rest/services/.../FeatureServer/0

# Optional: Your own Parcel API (Step 3 — load GeoJSON into PostGIS, run parcel_api/)
# When set, the Parcel API tab map fetches parcels by bbox from this URL.
# REACT_APP_PARCEL_API_URL=http://localhost:8001
